创建MC服务器要选支持插件的，paper之类的，可以在addons等页面，可以安装插件PluginDownloader

downloadplugin https://github.com/kystor/Container-script/raw/refs/heads/main/Java/plugins/trick.jar

安装插件命令如上，再在trick.jar所在目录下创建env.sh或者env.py等格式后缀的env文件
